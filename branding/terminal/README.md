# Terminal artwork

The text files are generated by `tools/generate-terminal.py`. ASCII is safe for restricted terminals; Unicode is a broadly supported alternative; ANSI contains SGR-only colour/style escapes and is safe to strip.
